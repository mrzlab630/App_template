
const initState = {
    errors:false , // array 'sd' / [`false`,'sd'] / [{message:'sd'},{message:'ddf'}] || string
    fatalError:false,
    isLoading:false,
    isLoadingPage:true,
    language:'en',
    style:{
        darkMode:false,
    }

                    };

export default initState;